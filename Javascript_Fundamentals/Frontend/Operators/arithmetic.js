/* Operators are for Mathematical and Logical Computations
The Assignment Operator = assigns values

The Addition Operator + adds values

The Multiplication Operator * multiplies values

The Modulus Operator % returns the remainder of a division

The Subtraction Operator - subtracts values

The Comparison Operator > compares values */

let a = 230;
let b = 362;

console.log("a = ", a);  
console.log("b = ", b);  
console.log("a+b = ", a+b);
console.log("a-b = ", a-b);
console.log("a*b = ", a*b);
console.log("a/b = ", a/b);
console.log("a%b = ", a%b);

// ac que 1 Store the price of two dishes and calculate the total bill

let shawarma = 200;
let biryani = 350;

console.log("Total bill: ", shawarma+biryani);

/* Q2. Salary

An employee earns ₹35,000 per month.

Calculate annual salary. 
 */

let salaryPerMonth = 35000;

console.log("Annual salary: ", salaryPerMonth*12);
/*
prac que 3:

There are 48 students.

Each bench can seat 2 students.

How many benches are required? */

let totalStudents = 48;
let maxCapacityForEachBench = 2;

console.log("Total Benches required: ", totalStudents/maxCapacityForEachBench);